[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short
markers =
    auth: Authentication tests
    authz: Authorization tests
    validation: Input validation tests
    parity: Functional parity tests
